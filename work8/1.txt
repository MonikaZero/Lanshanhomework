TS和JS的区别：

类型系统：JavaScript（JS）是动态类型语言，而TypeScript（TS）是静态类型语言。这意味着在TS中，您需要在编译时知道所有的变量类型，而在JS中，您可以在运行时更改变量的类型。
编译：JS是解释型语言，而TS需要编译成JS。这意味着TS代码需要先通过编译器转换为JS，然后由JS引擎执行。
强类型检查：TS提供了强类型检查，这有助于在开发阶段捕获许多错误，而在JS中这些错误可能会在运行时出现。
ES6+特性支持：TS内置了对ES6+特性的支持，这使得使用这些新特性更为方便。